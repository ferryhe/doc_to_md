# NOTE
# This file is a legacy GPU-oriented environment snapshot.
# It is not a single supported all-engine environment: fast-moving optional
# tools can impose mutually incompatible dependency ranges. Prefer isolated
# extras plus constraints and the scheduled dependency-resolution workflow.
#
# ============================================================
# requirements.txt — legacy GPU dependency snapshot
#
# Covers the shared base, Docling, and historical GPU dependencies. Install
# current optional engine packages through their pyproject extras instead.
#
# Install:  pip install -r requirements.txt
#
# For a lighter environment without GPU/PaddleOCR/MinerU/Marker
# use requirements-core.txt instead.
# ============================================================

--extra-index-url https://download.pytorch.org/whl/cu121

# ── core / project infrastructure ───────────────────────────
annotated-types==0.7.0
anyio==4.11.0
attrs==25.4.0
certifi==2025.11.12
charset-normalizer==3.5.1
click==8.3.3
colorama==0.4.6
colorlog==6.10.1
distro==1.9.0
eval_type_backport==0.3.0
filetype==1.2.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
Jinja2==3.1.6
jiter==0.12.0
lxml==6.0.2
markdown-it-py==4.2.0
marko==2.2.1
MarkupSafe==3.0.3
mdurl==0.1.2
packaging==25.0
pluggy==1.6.0
pydantic==2.12.4
pydantic-settings==2.15.0
pydantic_core==2.41.5
Pygments==2.19.2
python-dotenv==1.0.1
pytz==2025.2
regex==2026.7.19
requests==2.32.3
rich==15.0.0
rpds-py==0.29.0
setuptools==80.9.0
shellingham==1.5.4
six==1.17.0
sniffio==1.3.1
tabulate==0.9.0
tiktoken==0.7.0
typer==0.26.8
typing-inspection==0.4.4
typing_extensions==4.15.0
tzdata==2025.2
urllib3==2.5.0

# ── local engine (PDF · DOCX · images · text) ───────────────
# Reads PDF pages, Word documents, and drives pytesseract OCR.
beautifulsoup4==4.14.2
pillow==12.2.0
pypdf==6.13.0
pytesseract==0.3.13
python-docx==1.2.0
soupsieve==2.8

# ── office extra  (PPTX · XLSX)  [pip install ".[office]"] ──
# python-pptx reads PowerPoint; openpyxl reads Excel workbooks.
et_xmlfile==2.0.0
openpyxl==3.1.5
python-pptx==1.0.2

# ── html_local engine ───────────────────────────────────────
# beautifulsoup4 / lxml already listed above.
# For best-quality article extraction install trafilatura:
#   pip install ".[html]"   →  trafilatura>=1.6,<2

# ── mistral engine ──────────────────────────────────────────
# Requires MISTRAL_API_KEY in .env
mistralai==2.9.4

# ── mathpix engine ──────────────────────────────────────────
# Requires MATHPIX_APP_ID and MATHPIX_APP_KEY in .env.
# Uses `requests` from the base stack; no extra Python package is needed.

# ── deepseekocr engine (SiliconFlow API) ────────────────────
# Requires SILICONFLOW_API_KEY in .env; pypdfium2 renders PDFs
# to images for the vision model.
openai==2.54.0
pypdfium2==4.30.0

# ── markitdown engine  [pip install ".[markitdown]"] ────────
# markitdown itself is not pinned here; install via extra.

# ── docling engine  [pip install ".[docling]"] ───────────────
# Transformer-based document parser. Shares the GPU torch stack
# with paddleocr / mineru / marker below.
accelerate==1.11.0
antlr4-python3-runtime==4.9.3
dill==0.4.0
docling==2.123.0
docling-core==2.92.0
docling-ibm-models==3.14.0
docling-parse==7.16.0
Faker==38.0.0
filelock==3.19.1
fsspec==2025.9.0
huggingface-hub==0.36.0
invoke==2.2.1
jsonlines==4.0.0
jsonpath-python==1.1.6
jsonref==1.1.0
jsonschema==4.25.1
jsonschema-specifications==2025.9.1
latex2mathml==3.78.1
mpire==2.10.2
mpmath==1.3.0
multiprocess==0.70.18
networkx==3.5
numpy==2.2.6
omegaconf==2.3.0
pandas==2.3.3
polyfactory==3.0.0
psutil==7.1.3
pylatexenc==2.10
python-dateutil==2.9.0.post0
PyYAML==6.0.3
rapidocr==3.9.2
referencing==0.37.0
rtree==1.4.1
safetensors==0.6.2
scipy==1.16.3
semchunk==2.2.2
shapely==2.1.2
sympy==1.14.0
tokenizers==0.22.1
tqdm==4.67.1
transformers==4.57.1
tree-sitter==0.26.0
tree-sitter-c==0.24.1
tree-sitter-java==0.23.5
tree-sitter-javascript==0.25.0
tree-sitter-python==0.25.0
tree-sitter-typescript==0.23.2
xlsxwriter==3.2.9

# ── paddleocr engine  [pip install ".[paddleocr]"] ───────────
# Requires CUDA GPU. opencv-python and pyclipper are
# PaddleOCR-specific; pywin32 is required on Windows only.
opencv-python==4.12.0.88
pyclipper==1.3.0.post6
pywin32==311

# ── GPU / torch (CUDA 12.1) ──────────────────────────────────
# Shared by docling, paddleocr, mineru, and marker.
# Replace with the appropriate +cuXXX wheel for your CUDA
# version: https://download.pytorch.org/whl/
# For CPU-only (docling without GPU) use requirements-core.txt.
torch==2.2.2+cu121
torchaudio==2.2.2+cu121
torchvision==0.17.2+cu121
